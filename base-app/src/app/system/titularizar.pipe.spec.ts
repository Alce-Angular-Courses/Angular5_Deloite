import { TitularizarPipe } from './titularizar.pipe';

describe('TitularizarPipe', () => {
  it('create an instance', () => {
    const pipe = new TitularizarPipe();
    expect(pipe).toBeTruthy();
  });
});
